{% extends 'base.html.twig' %}

{% block title %}List of read books 📚{% endblock %}

{% block body %}
<table>
	<tr>
        <td>title</td>
		<td>author</td>
		<td>type</td>
		<td>note</td>
		<td>finished date</td>
		<td></td>
	</tr>

	{% include 'new.html.twig' %}

    {% for book in books %}
    <tr>
        <td>
            <strong>{{ book.title }}</strong>
        </td>

        <td>
            <strong>{{ book.author }}</strong>
        </td>

        <td>{{ book.type }}</td>
        <td>{{ book.note }}</td>
        <td>{{ book.finished_at|date('Y⋅m⋅d') }}</td>
        <td><a href="edit.php?s={{ book.slug }}" class="edit">🖍️</a> </td>
    </tr>
    {% endfor %}
</table>

{% endblock%}
